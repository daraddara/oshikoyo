# 課題管理 (Issue Management)

プロジェクトの動作仕様が曖昧な点や、今後検討が必要な項目をリスト化しています。技術的な決定事項や運用ルールについては、`.agents/rules/` 内のドキュメントが唯一の真実（SSOT）となります。

## 現在の課題・検討事項

| ID | 項目 | 内容 | ステータス |
|:---|:---|:---|:---|
| A-03 | ブラウザストレージの永続性 | `localStorage`がクリアされた際、デフォルト値に戻るが、その際ユーザーに通知するかどうか。 | 検討中 |
| A-04 | モバイル表示の最適化 | デスクトップPWAを主眼においていますが、極端に狭い画面（スマホ等）での縦スクロール/横スクロールの優先順位。 | 検討中 |
| A-05 | 過去・未来の境界 | 何年前まで、または何年先までカレンダーを遡れる/進められるか。 | 未着手 |
| A-06 | ダークモード切替UI | `design-standards.md` にダークモード対応の規定がありますが、現在UI上に切り替えスイッチが実装されていません。 | 未着手 |
| A-07 | 祝日データの外部API取得 | 将来的な法改正に対し、ユーザー自身が外部APIから最新の祝日データを取得しアップデートできるオプション機能の提供。 | 未着手 |
| A-13 | ユニットテストのモック堅牢性向上 | `tests/security.test.js` 等で `script.js` のコードを直接抽出（extractCode）し、JSDOMでモックしている仕組みが脆弱なため、ロジック変更やレイアウト関数の追加でテストが壊れやすい問題を改善する。 | 未着手 |

## 改善要望・機能追加案（未着手）

- [ ] 推しの編集画面（`oshiEditModal`）のUX改善
    - **要望**: 推し画像（アバター）の登録・変更機能の追加。現状は名前・カラー・記念日・タグのみ編集可能。
    - [x] **要望**: モバイルでの操作性向上（入力フィールドのサイズ調整、2段レイアウト化、追加ボタンの最適化など）。
    - **要望**: 記念日タイプ入力欄の候補表示UXの見直し（datalist の再クリック時挙動改善など）。


- [ ] 画像の魅力を高める「背景ぼかし（シアターモード）」の導入
    - **要望**: 画面左右などの余白に、同じ画像を拡大して強くぼかしたものを背景として表示し、没入感を高める。
- [ ] カレンダー全体を透過させる「全画面壁紙モード」の追加
    - **要望**: 画像を画面全体の背景（Wallpaper）として固定表示し、ガラスモーフィズムのUI越しに透けて見えるようにする。
- [ ] 印刷用CSSの最適化

---

## 解決済みの課題 (Resolved Issues)

| ID | 項目 | 内容 |
|:---|:---|:---|
| A-01 | 祝日ロジックの保守 | 祝日計算の実装（振替休日・特例対応含む）を完了。 |
| A-02 | 12ヶ月以上の表示 | 最大2ヶ月に制限し、レイアウトを安定化。 |
| A-08 | デフォルト画像の表示 | 初期表示時の画像読み込み問題を修正。 |
| A-09 | 設定画面のUX改善 | 項目整理と重複排除、リセット機能の実装。 |
| A-10 | カレンダー縦並び時の幅 | 縦レイアウト時の画像エリアの伸縮不具合を修正。 |
| A-11 | 表示モードの直接切替 | 画像エリアからの直接操作（固定・ランダム・サイクル）を実装。 |
| A-12 | テスト用画像の拡充 | 各種アスペクト比の Fixtures 導入と検証強化。 |
| A-14 | 全データバックアップ | Gzip圧縮による一括エクスポート・インポート機能を実装。 |
| A-15 | ストレージ容量管理 | 画像自動圧縮および一括圧縮機能を実装。 |
| A-16 | 画像タグパッケージ形式 | 画像とタグのみの抽出エクスポート機能を実装。 |
| A-17 | 推し情報のCSVインポート | CSVファイルからの推し情報一括登録を実装。 |
| A-18 | タグ管理基盤 | 画像および推しに対するタグ付け・フィルタリング機能を実装。 |

### 実装済みの機能追加
- [x] カスタム休日の設定機能
- [x] 任意のコンテンツ表示領域（推し画像）の追加
- [x] 画像・カレンダー配置の直感的UI操作（ドラッグ＆ドロップ、スプリッター）
- [x] 複数月表示時の縦横レイアウト直感操作
- [x] 画像切り替え間隔のプリセット化と特定時刻更新
- [x] スマホからの画像登録UX改善（Web Share Target & クリップボード）
- [x] 画像・推しへのタグ付け機能

---

## ブラウザ起因の既知制約

### Android Edge — Web Share Target 非対応

| 項目 | 内容 |
|---|---|
| 現象 | Android Edge にインストールした PWA が、他アプリの共有先一覧に表示されない |
| 原因 | Edge for Android が Web Share Target API を Android の Intent システムに登録しない（ブラウザ未実装） |
| 影響範囲 | Android Edge ユーザーのみ。Chrome インストール時は正常動作 |
| アプリ側の対応 | 設定画面の「画像を追加」ボタンによるファイル選択で代替可能 |
| コード側の対応 | 不要（manifest・SW の実装は正しい。ブラウザ制限のため修正不可） |
- [x] 全データバックアップとストレージ最適化
- [x] CSV形式での「推し」情報一括登録
